# 2026-08-15

## skill-consolidator（吸星大法）v2.0 重构

用户对自有 skill 不满意，要求用 skill-creator 重构。诊断出核心问题：该"整理 skills 的 skill"自身重复（中英两个包 + .trae 副本共 4 份 SKILL.md）、frontmatter 不合规（中文名、缺 agent_created、声明了不存在的 node/npm 依赖）、无确定性脚本、postinstall 副作用强、占位符未填。

用户确认三方向后完成重构：
- **合并为单个 skill**：`skill-consolidator/`（中文为主、双语触发词），删除 `吸星大法/` 与 `.trae/`（备份在 /tmp/skill-refactor-backup/，注意：原 .trae 副本是 29 字节损坏文件）
- **新增确定性脚本**（Node >= 18，.mjs）：
  - `skill-consolidator/scripts/scan_skills.mjs` — 扫描 TARGETS 表中的各 Agent skills 目录（P0-P3），解析 frontmatter，检测同名冲突/功能重叠/触发词冲突/版本差异，分类归档，输出 Markdown 报告或 JSON。已调优：扩充停用词、同名对不重复上报、P3 插件缓存间从严（共享关键词 ≥2）、重叠展示上限 40
  - `skill-consolidator/scripts/install_skills.mjs` — 手动安装器（复制整个包含 scripts，替代旧 postinstall）
  - `scripts/validate.mjs`（根级）— CI 与 `npm run prepublishOnly` 共用校验
- **发布配置修复**：package.json 移除 postinstall 自动副作用；CI workflow 重写复用 validate.mjs；README 改为单一源安装说明
- 端到端验证：用户机器上扫出 30 个 skill，3 处同名冲突（weixinpay 新旧版本缓存重复）、1 处版本差异、31 处功能重叠、1 处触发词冲突（tencent-docs ↔ tencent-saas-docs 共享 /word /excel）

待用户处理：`<your-username>` 占位符需替换为真实 GitHub 用户名（SKILL.md、_meta.json、homepage）。

## 后续修复
- 按 review 意见（sue1）：`scan_skills.mjs` 中 `import { createHash }` 原位于工具函数定义中间，已移到文件顶部与其他 import 合并（第 18 行），语法检查与冒烟测试通过。

## 发布前审计（GitHub）
- 新增 `.gitignore`（node_modules/.DS_Store/.workbuddy/日志/生成的报告）
- 移除 `scan_skills.mjs` 中未使用的 `basename` 导入
- LICENSE(MIT 2026)、CI、无旧引用残留均确认
- 唯一待办：`<your-username>` 占位符（_meta.json ownerId、SKILL.md homepage、README 安装命令）需用户提供 GitHub 用户名后替换

## 占位符已替换（GitHub 用户名: alancouny）
- `_meta.json` ownerId、`SKILL.md` homepage、README/README_EN 的 `npx skills add` 命令全部替换为真实值
- `package.json` 补充 `author` / `repository` / `homepage` / `bugs` 字段
- 校验全绿，无占位符残留，可发布

## git 初始化与首次提交
- 发现 `.git` 已被 IDE 自动初始化，且 `.workbuddy/memory/` 在 .gitignore 生效前已被暂存（已入 index 的文件不受 ignore 管辖），用 `git rm --cached` 移除，磁盘文件保留
- 仓库级配置 user.name=alancouny / user.email=alancouny@users.noreply.github.com（GitHub 隐私邮箱）
- 首次提交 `d563f6e`（main，13 文件 1129 行）
- 待用户：创建 GitHub 仓库 alancouny/skill-consolidator 后 add remote + push
